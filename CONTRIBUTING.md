## Contributing

Thanks for contributing!

- Keep changes **public-safe**: no secrets, private endpoints, logs, PII, or production runbooks.
- Prefer adding tests under `tests/`.
- If you add configuration, update `configs/.env.example` (never commit real `.env` files).

TODO: Add lint/test commands and CI expectations.

