"""Security helpers (public-safe).

This package provides optional bearer-token auth used by Memory/Vector HTTP pods.
The public tree keeps auth intentionally minimal: a single shared token gate,
disabled by default.
"""

